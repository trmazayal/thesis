apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: server
  labels:
    app: server
spec:
  minReplicas: 2
  maxReplicas: 16
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: server
  targetCPUUtilizationPercentage: 90